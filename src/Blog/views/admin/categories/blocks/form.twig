{{ field(
    'name',
    'Titre de l\'article',
    item.name,
    {
        placeholder: 'Plats chauds'
    }
) }}
{{ field(
    'slug',
    'URI l\'article',
    item.slug,
    {
        placeholder: 'plat-chauds'
    }
) }}