{% extends '@admin/layout.twig' %}

{% block title "Ecrire un article" %}

{% block breadcrumb %}
    <li class="breadcrumb-item">
        <a href="{{ path(routePrefix ~ '.index') }}" class="tooltip" data-tooltip="Blog">Blog</a>
    </li>
    <li class="breadcrumb-item">
        <a href="{{ path(routePrefix ~ '.create') }}" class="tooltip" data-tooltip="{{ block('title') }}">{{ block('title') }}</a>
    </li>
{% endblock %}

{% block content %}

    <div class="container">

        <form action="{{ path(routePrefix ~ '.create') }}" method="post" enctype="multipart/form-data">
            {{ include(viewPath ~ '/blocks/form.twig') }}
            <button class="btn btn-primary mt-2 float-right" type="submit">Créer</button>
        </form>

    </div>

{% endblock %}
